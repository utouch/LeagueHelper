<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leaguehelper" >

    <uses-permission android:name="android.permission.USE_CREDENTIALS"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".AppContext"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activitys.BindAccountActivity"
            >
        </activity>
        <activity
            android:name=".activitys.TestActivity"
            >
        </activity>
        <activity
            android:name=".DrawerActivity"
            android:label="@string/title_activity_drawer" >
        </activity>
        <activity
            android:name=".ToolBarActivity" >
        </activity>


        <!--<service
            android:name=".TargetService"
            android:process=":remotetargetservice" >
        </service>

        <receiver android:name="com.leaguehelper.BootReceiver" >
            <intent-filter>
                <action android:name="BOOT_COMPLETED" />
            </intent-filter>
        </receiver>-->

    </application>

</manifest>
